server:
  port: 9091
spring:
  config:
    enabled: true
 # cloud:
  #  gateway:
   #   discovery:
    #    locator:
     #     lower-case-service-id: true
  application:
    name: EMPLOYEE-SERVICE

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
